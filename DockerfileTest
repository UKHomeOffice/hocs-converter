FROM quay.io/ukhomeofficedigital/nodejs-base:v8

RUN yum install -y unoconv make gcc*

WORKDIR /app
COPY . /app
COPY ./__tests__ /app/__tests__
RUN npm --loglevel warn install -g node-gyp jest
RUN npm --loglevel warn install --no-optional

USER 999

EXPOSE 8080

CMD /app/scripts/run-tests.sh
